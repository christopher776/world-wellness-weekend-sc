import{createCipheriv,createHash,randomBytes}from"node:crypto";import{list,put}from"@vercel/blob";import{marketplacePrice}from"@/lib/marketplace-pricing";export const MARKETPLACE_ITEM_PREFIX="sc-wellness-marketplace/items/";export const MARKETPLACE_CLAIM_PREFIX="sc-wellness-marketplace/claims/";export interface MarketplaceItem{ID:string;Title:string;PhotoURL:string;Description:string;DonorName:string;RetailValue:string;LaunchAt:string;EndAt:string;Quantity:string;PickupDetails:string;Terms:string;TaxCategory:string;Featured:string;Published:string;UpdatedAt:string;}export interface MarketplaceAvailability{itemId:string;claimed:number;quantity:number;remaining:number;}export interface MarketplaceOrderPayload{orderId:string;itemId:string;itemTitle:string;price:number;retailValue:number;claimedAt:string;name:string;email:string;phone:string;}function truthy(value?:string){return["true","yes","1","y"].includes((value||"").trim().toLowerCase())}function safeId(value:string){return value.replace(/[^a-zA-Z0-9_-]/g,"-")}function requireStorage(){if(!process.env.BLOB_READ_WRITE_TOKEN&&!process.env.BLOB_STORE_ID)throw new Error("Marketplace storage is not configured.")}async function readJson<T>(url:string,uploadedAt?:Date):Promise<T>{const separator=url.includes("?")?"&":"?";const response=await fetch(`${url}${separator}v=${uploadedAt?.getTime()||Date.now()}`,{cache:"no-store"});if(!response.ok)throw new Error("Marketplace listing storage could not be read.");return await response.json()as T}function isMarketplaceItem(value:unknown):value is MarketplaceItem{if(!value||typeof value!=="object")return false;const item=value as Partial<MarketplaceItem>;return typeof item.ID==="string"&&typeof item.Title==="string"&&typeof item.RetailValue==="string"&&Number(item.RetailValue)>0&&typeof item.LaunchAt==="string"&&typeof item.EndAt==="string"&&typeof item.Published==="string"}export async function fetchMarketplaceItems(options:{publishedOnly?:boolean}={}){requireStorage();const result=await list({prefix:MARKETPLACE_ITEM_PREFIX,limit:1000});const items=(await Promise.all(result.blobs.map(blob=>readJson<unknown>(blob.url,blob.uploadedAt)))).filter(isMarketplaceItem).sort((a,b)=>Number(b.Featured==="TRUE")-Number(a.Featured==="TRUE")||a.Title.localeCompare(b.Title));return options.publishedOnly?items.filter(item=>truthy(item.Published)):items}export async function saveMarketplaceItem(item:MarketplaceItem){requireStorage();await put(`${MARKETPLACE_ITEM_PREFIX}${safeId(item.ID)}.json`,JSON.stringify(item),{access:"public",allowOverwrite:true,addRandomSuffix:false,contentType:"application/json",cacheControlMaxAge:60})}export async function marketplaceAvailability(items:MarketplaceItem[]){requireStorage();const claims=await list({prefix:MARKETPLACE_CLAIM_PREFIX,limit:1000});return items.map(item=>{const path=`${MARKETPLACE_CLAIM_PREFIX}${safeId(item.ID)}.enc`;const claimed=claims.blobs.some(blob=>blob.pathname===path)?1:0;return{itemId:item.ID,claimed,quantity:1,remaining:1-claimed}})}export async function marketplaceItemIsReserved(itemId:string){requireStorage();const path=`${MARKETPLACE_CLAIM_PREFIX}${safeId(itemId)}.enc`;const result=await list({prefix:path,limit:2});const exact=result.blobs.filter(blob=>blob.pathname===path);if(exact.length>1)throw new Error("Marketplace reservation state is inconsistent.");return exact.length===1}function encryptionKey(){const secret=process.env.ADMIN_API_SECRET||process.env.ADMIN_PASSWORD||"";if(!secret)throw new Error("Admin encryption secret is not configured.");return createHash("sha256").update(secret).digest()}function encryptOrder(payload:MarketplaceOrderPayload){const iv=randomBytes(12);const cipher=createCipheriv("aes-256-gcm",encryptionKey(),iv);const encrypted=Buffer.concat([cipher.update(JSON.stringify(payload),"utf8"),cipher.final()]);return JSON.stringify({iv:iv.toString("base64"),tag:cipher.getAuthTag().toString("base64"),data:encrypted.toString("base64")})}export async function claimMarketplaceItem(item:MarketplaceItem,contact:{name:string;email:string;phone:string}){requireStorage();const pricing=marketplacePrice(item,new Date());const orderId=`order-${Date.now().toString(36)}-${randomBytes(3).toString("hex")}`;const claimedAt=new Date().toISOString();const payload:MarketplaceOrderPayload={orderId,itemId:item.ID,itemTitle:item.Title,price:Number(pricing.price.toFixed(2)),retailValue:Number(item.RetailValue),claimedAt,name:contact.name.trim(),email:contact.email.trim().toLowerCase(),phone:contact.phone.trim()};const claimPath=`${MARKETPLACE_CLAIM_PREFIX}${safeId(item.ID)}.enc`;try{await put(claimPath,encryptOrder(payload),{access:"public",allowOverwrite:false,addRandomSuffix:false,contentType:"application/octet-stream",cacheControlMaxAge:60})}catch{return{ok:false as const,error:"This item was just reserved by someone else."}}return{ok:true as const,orderId,slot:1,price:payload.price,remaining:0}}
