# Preview verification

Preview deployment should be verified before merge. Because production credentials are intentionally absent from Preview unless deliberately added, checkout should remain in fallback mode there unless sandbox variables are configured.
