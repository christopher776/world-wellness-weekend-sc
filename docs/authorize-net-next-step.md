# Final activation dependency

The code can deploy safely before credentials are entered. Until Authorize.Net credentials are present, the reservation flow falls back to payment follow-up. Once credentials are present, Pay Now activates automatically.
