import{NextResponse}from"next/server";import{newId,upsertRow}from"@/lib/cms";import{fetchMarketplaceItems,marketplaceItemIsReserved,saveMarketplaceItem,type MarketplaceItem}from"@/lib/marketplace";import{parseCharlestonDateTime}from"@/lib/marketplace-pricing";export const runtime="nodejs";export async function GET(){try{return NextResponse.json({ok:true,items:await fetchMarketplaceItems()})}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Could not load marketplace."},{status:500})}}export async function POST(request:Request){let body:{id?:string;fields?:Partial<MarketplaceItem>;retailConfirmed?:boolean};try{body=await request.json()}catch{return NextResponse.json({ok:false,error:"Invalid request body."},{status:400})}const fields=body.fields||{};for(const field of["Title","Description","RetailValue","LaunchAt","EndAt"]as const){if(!String(fields[field]||"").trim())return NextResponse.json({ok:false,error:`${field} is required.`},{status:400})}const retail=Number(fields.RetailValue);if(!Number.isFinite(retail)||retail<=0)return NextResponse.json({ok:false,error:"Retail value must be greater than zero."},{status:400});const floorPercent=Number(fields.FloorPercent||15);if(!Number.isFinite(floorPercent)||floorPercent<=0||floorPercent>=100)return NextResponse.json({ok:false,error:"Final price floor must be greater than 0% and less than 100% of retail."},{status:400});const launch=parseCharlestonDateTime(String(fields.LaunchAt));const end=parseCharlestonDateTime(String(fields.EndAt));if(!launch||!end||end<=launch)return NextResponse.json({ok:false,error:"The end time must be after the launch time."},{status:400});let current:MarketplaceItem|undefined;const id=body.id?.trim()||newId("market");if(body.id){try{if(await marketplaceItemIsReserved(id))return NextResponse.json({ok:false,error:"This item is reserved and can no longer be edited. Resolve the reservation before changing the listing."},{status:409});current=(await fetchMarketplaceItems()).find(item=>item.ID===id);if(current){const currentLaunch=parseCharlestonDateTime(current.LaunchAt);const launched=Boolean(currentLaunch&&Date.now()>=currentLaunch.getTime());const pricingChanged=retail.toFixed(2)!==Number(current.RetailValue).toFixed(2)||floorPercent.toFixed(2)!==Number(current.FloorPercent||15).toFixed(2)||String(fields.LaunchAt)!==current.LaunchAt||String(fields.EndAt)!==current.EndAt;const coreChanged=String(fields.Title||"").trim()!==current.Title||String(fields.Description||"").trim()!==current.Description||String(fields.PhotoURL||"").trim()!==current.PhotoURL||String(fields.DonorName||"").trim()!==current.DonorName||String(fields.Terms||"").trim()!==current.Terms||String(fields.PickupDetails||"").trim()!==current.PickupDetails||String(fields.TaxCategory||"Review at payment")!==current.TaxCategory;if(launched&&(pricingChanged||coreChanged))return NextResponse.json({ok:false,error:"Offer details are locked once the price drop begins. You may change Featured status or turn off Published without changing the offer."},{status:409})}}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Could not verify the existing listing."},{status:502})}}
if(fields.Published==="TRUE"){const retailChanged=!current||retail.toFixed(2)!==Number(current.RetailValue).toFixed(2);const firstPublish=!current||current.Published!=="TRUE";if((retailChanged||firstPublish)&&!body.retailConfirmed)return NextResponse.json({ok:false,error:"Confirm that the entered retail price is the genuine regular retail value before publishing."},{status:400});if(!String(fields.PhotoURL||"").trim())return NextResponse.json({ok:false,error:"A photo is required before a marketplace item can be published."},{status:400});if(!String(fields.DonorName||"").trim())return NextResponse.json({ok:false,error:"The contributing business / provider is required before publishing."},{status:400});if(String(fields.TaxCategory||"Review at payment")==="Review at payment")return NextResponse.json({ok:false,error:"Select the item tax category before publishing."},{status:400});if(!String(fields.PickupDetails||"").trim()||!String(fields.Terms||"").trim())return NextResponse.json({ok:false,error:"Fulfillment details and item terms are required before publishing."},{status:400})}
const item:MarketplaceItem={ID:id,Title:String(fields.Title||"").trim(),PhotoURL:String(fields.PhotoURL||"").trim(),Description:String(fields.Description||"").trim(),DonorName:String(fields.DonorName||"").trim(),RetailValue:retail.toFixed(2),LaunchAt:String(fields.LaunchAt||""),EndAt:String(fields.EndAt||""),FloorPercent:String(Number(floorPercent.toFixed(2))),Quantity:"1",PickupDetails:String(fields.PickupDetails||"").trim(),Terms:String(fields.Terms||"").trim(),TaxCategory:String(fields.TaxCategory||"Review at payment"),Featured:fields.Featured==="TRUE"?"TRUE":"FALSE",Published:fields.Published==="TRUE"?"TRUE":"FALSE",UpdatedAt:new Date().toISOString()};try{await saveMarketplaceItem(item);await upsertRow("MarketplaceItems",{ID:item.ID,Title:item.Title,PhotoURL:item.PhotoURL,Description:item.Description,DonorName:item.DonorName,RetailValue:item.RetailValue,LaunchAt:item.LaunchAt,EndAt:item.EndAt,FloorPercent:item.FloorPercent,Inventory:"1 unique item per listing",PickupDetails:item.PickupDetails,Terms:item.Terms,TaxCategory:item.TaxCategory,Featured:item.Featured,Published:item.Published,UpdatedAt:item.UpdatedAt}).catch(()=>({ok:false}));return NextResponse.json({ok:true,id})}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Marketplace item could not be saved."},{status:502})}}
