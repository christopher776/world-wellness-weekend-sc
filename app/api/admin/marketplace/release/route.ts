import{NextResponse}from"next/server";import{getMarketplaceOrder,releaseMarketplaceItem}from"@/lib/marketplace";import{syncMarketplaceSale}from"@/lib/marketplace-sales";export const runtime="nodejs";export async function POST(request:Request){let body:{itemId?:string;reason?:string};try{body=await request.json()}catch{return NextResponse.json({ok:false,error:"Invalid request body."},{status:400})}const itemId=body.itemId?.trim();if(!itemId)return NextResponse.json({ok:false,error:"Item ID is required."},{status:400});try{const order=await getMarketplaceOrder(itemId);if(order?.paymentStatus==="PAID")return NextResponse.json({ok:false,error:"This order is PAID and cannot be released as an unpaid/test reservation. Refund or void the Authorize.Net transaction first."},{status:409});const released=await releaseMarketplaceItem(itemId);if(!released)return NextResponse.json({ok:false,error:"This item is not currently reserved."},{status:409});const reason=body.reason?.trim()||"Sale fell through / testing reset";if(order)await syncMarketplaceSale(order,"RELEASED",{ReleaseReason:reason,ReleasedAt:new Date().toISOString()}).catch(()=>undefined);const url=process.env.GOOGLE_SHEETS_WEBHOOK_URL;if(url&&order){await fetch(url,{method:"POST",headers:{"Content-Type":"application/json","User-Agent":"Mozilla/5.0 Chrome/124 Safari/537.36"},body:JSON.stringify({type:"marketplace-release",name:order.name,email:order.email,phone:order.phone,organization:order.itemTitle,interest:`Released ${order.orderId}`,message:`Reservation released by admin. Reason: ${reason}. Previous locked price: $${order.price.toFixed(2)}.`}),redirect:"follow"}).catch(()=>undefined)}return NextResponse.json({ok:true,released:true,orderId:order?.orderId||null})}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Could not release marketplace item."},{status:502})}}
