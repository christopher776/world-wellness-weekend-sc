import{NextResponse}from"next/server";import{upsertRow}from"@/lib/cms";import{fetchMarketplaceItems,saveMarketplaceItem}from"@/lib/marketplace";export const runtime="nodejs";export async function POST(request:Request){let body:{itemId?:string;published?:boolean};try{body=await request.json()}catch{return NextResponse.json({ok:false,error:"Invalid request body."},{status:400})}const itemId=String(body.itemId||"").trim();if(!itemId)return NextResponse.json({ok:false,error:"Item ID is required."},{status:400});try{const items=await fetchMarketplaceItems();const current=items.find(item=>item.ID===itemId);if(!current)return NextResponse.json({ok:false,error:"Marketplace item not found."},{status:404});const item={...current,Published:body.published?"TRUE":"FALSE",UpdatedAt:new Date().toISOString()};await saveMarketplaceItem(item);await upsertRow("MarketplaceItems",{ID:item.ID,Title:item.Title,PhotoURL:item.PhotoURL,Description:item.Description,DonorName:item.DonorName,RetailValue:item.RetailValue,LaunchAt:item.LaunchAt,EndAt:item.EndAt,FloorPercent:item.FloorPercent,Inventory:"1 unique item per listing",PickupDetails:item.PickupDetails,Terms:item.Terms,TaxCategory:item.TaxCategory,Featured:item.Featured,Published:item.Published,UpdatedAt:item.UpdatedAt}).catch(()=>({ok:false}));return NextResponse.json({ok:true,published:item.Published==="TRUE"})}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Could not update public visibility."},{status:502})}}